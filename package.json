{
  "name": "spamtrap-bot",
  "version": "1.0.0",
  "description": "Spam Trap: catches spam bots and compromised accounts using a trap channel. Any message in the trap triggers an automatic soft-ban with DM notification. Configurable via /spamtrap.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "discord.js": "^14.16.3",
    "dotenv": "^16.4.7",
    "node-cron": "^3.0.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
