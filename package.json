{
  "name": "spam-trap-bot",
  "version": "3.0.0",
  "description": "Discord moderation bot with web dashboard. Catches spam bots and compromised accounts via trap channels.",
  "main": "index.js",
  "scripts": {
    "start": "node deploy-commands.js && node index.js",
    "deploy-commands": "node deploy-commands.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cookie-session": "^2.1.0",
    "discord.js": "^14.16.3",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
