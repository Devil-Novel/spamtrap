{
  "name": "spam-trap-bot",
  "version": "1.0.0",
  "description": "Discord moderation bot that catches spam bots and compromised accounts via a trap channel",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "deploy-commands": "node deploy-commands.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "discord.js": "^14.16.3",
    "dotenv": "^16.4.5"
  }
}
